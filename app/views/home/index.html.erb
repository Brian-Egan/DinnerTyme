 <div class="formHeader">
    <%= link_to 'New Recipe', new_recipe_path, :class => 'btn btn-large btn-success' %>
 	<%= link_to 'New Kitchen', new_kitchen_path, :class => 'btn btn-large btn-success' %>
    <%= link_to 'New Ingredient', new_ingredient_path, :class => 'btn btn-large btn-success' %>  
 </div>
  <div id="page-content">
      <div class="page-header">
        <h1>Welcome <small>to the new home of delicious</small>
        	<p>Explore one of our existing recipes, or create one of your own!</p>
        </h1>
     
      

</div>


      <ul class="thumbnails">
      	<% Recipe.all.each do |r| %>
      	 <li class="span4">
          <div class="thumbnail">
	      	<%= r.name %>
	      	<%=link_to( image_tag(r.photo), recipes_path) %>
	      	
	      </div>
	      </li>
	        <% end %>
      </ul>
    </div>



  </body>

</html>